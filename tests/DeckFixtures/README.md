# Native deck fixtures

The three DeckSpec files each exercise all twelve semantic layouts. Together they form the 36 theme/layout structural regression combinations. The SVG is the shared local media fixture; no remote assets are fetched during compilation.
